<%= form_with(model: trano, local: true) do |form| %>
  <% if trano.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trano.errors.count, "error") %> prohibited this trano from being saved:</h2>

      <ul>
      <% trano.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
